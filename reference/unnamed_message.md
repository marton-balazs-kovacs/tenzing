# Return messages as unnamed vector for js handler

The function filters the results of the validation of the
contributors_table template by type. Then the message elements are
pulled and collapsed into one element.

## Usage

``` r
unnamed_message(x, y)
```

## Arguments

- x:

  The result of the validation

- y:

  The type of validation message

## Value

Returns a character vector with one element.

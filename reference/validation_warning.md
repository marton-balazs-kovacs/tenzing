# Create a warning validation result

Create a warning validation result

## Usage

``` r
validation_warning(message, affected_rows = NULL, details = NULL)
```

## Arguments

- message:

  Character string with the warning message.

- affected_rows:

  Optional vector of affected row numbers.

- details:

  Optional list with additional details.

## Value

A standardized warning validation result.

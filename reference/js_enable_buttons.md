# Enable buttons by selector

Note: The underlying JS handler is named "reable" (typo) but this
wrapper provides the correct name "enable" for better clarity.

## Usage

``` r
js_enable_buttons(selector)
```

## Arguments

- selector:

  CSS selector for buttons to enable

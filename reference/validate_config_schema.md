# Validate YAML configuration structure

Validates that a YAML configuration has the required structure and
fields.

## Usage

``` r
validate_config_schema(config)
```

## Arguments

- config:

  Configuration list to validate

## Value

TRUE if valid, FALSE otherwise

# Validate column configuration schema

Validate column configuration schema

## Usage

``` r
validate_column_config_schema(column_config)
```

## Arguments

- column_config:

  Column configuration to validate

## Value

TRUE if valid, FALSE otherwise
